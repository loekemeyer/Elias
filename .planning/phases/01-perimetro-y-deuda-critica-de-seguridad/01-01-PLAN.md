---
phase: 01-perimetro-y-deuda-critica-de-seguridad
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - package.json
  - supabase-admin-client.js
  - check-non-webp.js
  - compress-75kb.js
  - download-all-images.js
  - optimize-images.js
  - recreate-bucket.js
  - rename-images.js
  - storage-audit.js
  - storage-size.js
autonomous: true
requirements: [SEC-01]

must_haves:
  truths:
    - "Ningun script de mantenimiento contiene la service_role key en texto plano"
    - "Los 8 scripts leen la key desde process.env.SUPABASE_SERVICE_ROLE_KEY"
    - "Correr un script sin la variable de entorno definida falla con mensaje claro, no con error opaco de Supabase"
    - "Correr un script con la variable definida sigue funcionando igual que antes"
  artifacts:
    - path: "supabase-admin-client.js"
      provides: "Cliente Supabase admin compartido con fail-fast si falta la env var"
      exports: ["createAdminClient", "SUPABASE_URL"]
    - path: ".env.example"
      provides: "Plantilla de variables de entorno sin secretos reales"
      contains: "SUPABASE_SERVICE_ROLE_KEY="
  key_links:
    - from: "storage-audit.js"
      to: "supabase-admin-client.js"
      via: "require del helper compartido"
      pattern: "require\\(\"\\./supabase-admin-client\"\\)"
    - from: "supabase-admin-client.js"
      to: "process.env.SUPABASE_SERVICE_ROLE_KEY"
      via: "lectura de variable de entorno con guard"
      pattern: "process\\.env\\.SUPABASE_SERVICE_ROLE_KEY"
---

<objective>
Sacar la `service_role` key de Supabase del codigo fuente de los 8 scripts de mantenimiento de storage y pasarla a variable de entorno, con un helper compartido que falle rapido y claro si la variable no esta definida.

Purpose: SEC-01. Es el prerrequisito duro de SEC-02 (rotacion). Si se rota la key antes de este plan, los 8 scripts quedan rotos en silencio y nadie se entera hasta la proxima vez que alguien corra uno.

Output: `supabase-admin-client.js` (helper nuevo), `.env.example`, `package.json` con `dotenv`, y los 8 scripts refactorizados sin ningun secreto en texto plano.

**Ejecutor: Claude.** Este plan es 100% autonomo, no requiere nada del usuario salvo tener `node` disponible.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md

@storage-audit.js
@check-non-webp.js
@package.json
@.gitignore

<interfaces>
<!-- Patron actual, identico en los 8 scripts. Extraido del codebase. -->
<!-- El ejecutor NO necesita explorar: este es el bloque exacto a reemplazar. -->

Patron actual (ejemplo `storage-audit.js:9-15`):
```js
const { createClient } = require("@supabase/supabase-js");

const SUPABASE_URL = "https://zjvpzqhbekxnwxdczpof.supabase.co";
const SUPABASE_KEY =
  "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....kPO2Ku3lAI-c5wC7STD-AIRcI6ww9PKG60Vsn_UJIu4";

const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
```

Linea exacta de la constante `SUPABASE_KEY` por archivo (verificado 2026-07-21):

| Archivo | Linea |
|---|---|
| `check-non-webp.js` | 13 |
| `compress-75kb.js` | 17 |
| `download-all-images.js` | 17 |
| `optimize-images.js` | 20 |
| `recreate-bucket.js` | 20 |
| `rename-images.js` | 16 |
| `storage-audit.js` | 12 |
| `storage-size.js` | 12 |

Notas de variacion entre archivos (no asumir bloques identicos byte a byte):
- Algunos declaran constantes extra entre `SUPABASE_KEY` y `createClient` (ej. `check-non-webp.js:15` declara `BUCKET`). **Preservarlas.**
- Algunos importan mas modulos antes (`sharp`, `fs`, `path`, `mime-types`). **Preservarlos.**
- Todos usan el nombre de variable local `supabase` para el cliente. **Preservar ese nombre** para no tocar el resto del cuerpo del script.

`package.json` actual (completo):
```json
{
  "dependencies": {
    "@supabase/supabase-js": "^2.101.0",
    "sharp": "^0.34.5"
  }
}
```

`.gitignore` YA cubre credenciales (lineas 15-18): `.env`, `.env.*`, `*.pat`.
**No hace falta modificar `.gitignore`.** Solo verificar que sigue asi.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Crear helper de cliente admin y plantilla de entorno</name>
  <files>supabase-admin-client.js, .env.example, package.json</files>
  <action>
Crear `supabase-admin-client.js` en la raiz del repo. Modulo CommonJS (el resto del repo usa `require`, mantener el estilo). Contenido funcional:

```js
/**
 * supabase-admin-client.js
 *
 * Cliente Supabase con service_role para los scripts de mantenimiento de storage.
 * La key NO vive en el codigo: se lee de la variable de entorno
 * SUPABASE_SERVICE_ROLE_KEY (ver .env.example).
 *
 * Uso:
 *   const { createAdminClient, SUPABASE_URL } = require("./supabase-admin-client");
 *   const supabase = createAdminClient();
 */

require("dotenv").config();

const { createClient } = require("@supabase/supabase-js");

const SUPABASE_URL =
  process.env.SUPABASE_URL || "https://zjvpzqhbekxnwxdczpof.supabase.co";

function createAdminClient() {
  const key = process.env.SUPABASE_SERVICE_ROLE_KEY;
  if (!key) {
    console.error(
      "\n[ERROR] Falta la variable de entorno SUPABASE_SERVICE_ROLE_KEY.\n" +
        "  1. Copiar .env.example a .env\n" +
        "  2. Pegar la service_role key del dashboard de Supabase\n" +
        "     (Project Settings > API > service_role)\n" +
        "  El archivo .env esta en .gitignore y NO se versiona.\n"
    );
    process.exit(1);
  }
  return createClient(SUPABASE_URL, key);
}

module.exports = { createAdminClient, SUPABASE_URL };
```

`SUPABASE_URL` mantiene el default hardcodeado a proposito: la URL del proyecto no es un secreto (ya esta en `script.js`, `admin.js` e `historial.js` del frontend publico) y dejarla por default evita romper los scripts por una variable faltante que no aporta seguridad.

Crear `.env.example` en la raiz:

```
# Credenciales de los scripts de mantenimiento de storage.
# Copiar este archivo a .env y completar. .env NO se versiona (.gitignore).

# URL del proyecto Supabase de Tierra Nativa. Opcional: si no se define,
# el helper usa https://zjvpzqhbekxnwxdczpof.supabase.co
SUPABASE_URL=https://zjvpzqhbekxnwxdczpof.supabase.co

# service_role key. Dashboard de Supabase > Project Settings > API > service_role.
# BYPASSEA TODO EL RLS DEL PROYECTO. Nunca commitear, nunca mandar por chat.
SUPABASE_SERVICE_ROLE_KEY=
```

Agregar `dotenv` a `package.json` dependencies (queda: `@supabase/supabase-js`, `dotenv`, `sharp`, en orden alfabetico). Usar `"dotenv": "^17.0.0"`.

Correr `npm install` para materializar `node_modules/dotenv` y actualizar `package-lock.json`.

**No modificar `.gitignore`** — ya cubre `.env` y `.env.*` en las lineas 15-18. Solo verificar (ver `<verify>`).

**No crear ni completar el archivo `.env` real.** El usuario pega la key en el Plan 03, despues de rotarla. Ese es el punto: hoy la key vieja sigue siendo valida y no queremos escribirla en un archivo nuevo.
  </action>
  <verify>
    <automated>node -e "const m=require('./supabase-admin-client');if(typeof m.createAdminClient!=='function')process.exit(1);console.log('helper OK')" &amp;&amp; node -e "const p=require('./package.json');if(!p.dependencies.dotenv)process.exit(1);console.log('dotenv OK')" &amp;&amp; grep -qx '.env' .gitignore &amp;&amp; echo "gitignore OK" &amp;&amp; grep -c 'SUPABASE_SERVICE_ROLE_KEY' .env.example</automated>
  </verify>
  <done>
`supabase-admin-client.js` existe y exporta `createAdminClient` + `SUPABASE_URL`. `.env.example` existe con la clave `SUPABASE_SERVICE_ROLE_KEY=` vacia y sin ningun secreto real. `package.json` lista `dotenv`. `.gitignore` sigue conteniendo `.env`. `node_modules/dotenv` instalado. No existe archivo `.env`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Refactorizar los 8 scripts al helper compartido</name>
  <files>check-non-webp.js, compress-75kb.js, download-all-images.js, optimize-images.js, recreate-bucket.js, rename-images.js, storage-audit.js, storage-size.js</files>
  <action>
En cada uno de los 8 scripts, aplicar la misma transformacion. Es una edicion mecanica repetida; usar Edit por archivo, no reescribir el archivo entero.

Reemplazar:
```js
const { createClient } = require("@supabase/supabase-js");
...
const SUPABASE_URL = "https://zjvpzqhbekxnwxdczpof.supabase.co";
const SUPABASE_KEY =
  "eyJ...";
...
const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);
```

Por:
```js
const { createAdminClient } = require("./supabase-admin-client");
...
const supabase = createAdminClient();
```

Reglas de la edicion, en orden:

1. Eliminar la linea `const { createClient } = require("@supabase/supabase-js");` y reemplazarla por `const { createAdminClient } = require("./supabase-admin-client");` en la misma posicion.
2. Eliminar por completo las declaraciones `const SUPABASE_URL = "...";` y `const SUPABASE_KEY = "eyJ...";` (la segunda ocupa 2 lineas: nombre y string).
3. Reemplazar `const supabase = createClient(SUPABASE_URL, SUPABASE_KEY);` por `const supabase = createAdminClient();`.
4. **Preservar intactas** las demas constantes del preambulo (`BUCKET` en `check-non-webp.js:15`, y las que tengan los otros scripts) y los demas `require` (`sharp`, `fs`, `path`, `mime-types`).
5. Si algun script usa `SUPABASE_URL` mas abajo en el cuerpo (para armar URLs publicas de storage), NO borrar la referencia: importar la constante del helper — `const { createAdminClient, SUPABASE_URL } = require("./supabase-admin-client");`. Grepear cada archivo por `SUPABASE_URL` antes de borrar la declaracion.
6. Actualizar el bloque de comentario `Uso:` de la cabecera de cada script para reflejar que ahora requiere `.env`. Ejemplo: `Uso: node storage-audit.js   (requiere .env con SUPABASE_SERVICE_ROLE_KEY)`.

No tocar ninguna otra logica de ningun script.
  </action>
  <verify>
    <automated>! grep -rn "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9" --include=*.js . --exclude-dir=node_modules &amp;&amp; test $(grep -l "createAdminClient" check-non-webp.js compress-75kb.js download-all-images.js optimize-images.js recreate-bucket.js rename-images.js storage-audit.js storage-size.js | wc -l) -eq 8 &amp;&amp; ! grep -n "SUPABASE_KEY" check-non-webp.js compress-75kb.js download-all-images.js optimize-images.js recreate-bucket.js rename-images.js storage-audit.js storage-size.js &amp;&amp; echo "8/8 refactorizados, 0 secretos"</automated>
  </verify>
  <done>
Los 8 scripts requieren `./supabase-admin-client` y llaman `createAdminClient()`. Grep del prefijo del JWT (`eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9`) sobre todo el repo excluyendo `node_modules` devuelve cero coincidencias. Ninguno de los 8 declara `SUPABASE_KEY`. Las constantes propias de cada script (`BUCKET`, etc.) siguen presentes.
  </done>
</task>

<task type="auto">
  <name>Task 3: Smoke test de los dos caminos (con y sin env var)</name>
  <files>(ninguno — solo ejecucion y verificacion)</files>
  <action>
Verificar que el refactor no rompio nada, probando los dos caminos.

**Camino A — sin variable de entorno (debe fallar limpio):**
Correr `node storage-audit.js` con `SUPABASE_SERVICE_ROLE_KEY` sin definir y sin `.env` presente. Debe imprimir el mensaje de error del helper y salir con codigo 1. NO debe tirar un stacktrace de `@supabase/supabase-js` ni un error de red opaco.

**Camino B — con variable de entorno (debe funcionar):**
Correr `storage-audit.js` pasando la key **vieja** por variable de entorno de la sesion, sin escribirla a disco:

```powershell
$env:SUPABASE_SERVICE_ROLE_KEY = "<key vieja, la que estaba hardcodeada>"
node storage-audit.js
$env:SUPABASE_SERVICE_ROLE_KEY = $null
```

La key vieja todavia es valida en este punto — la rotacion es el Plan 03. Debe listar los buckets con sus tamanos, igual que antes del refactor.

Limpiar la variable de la sesion al terminar (`$env:... = $null`). No dejarla seteada y no escribirla a ningun archivo.

**Verificacion por inspeccion de los otros 7:** no correrlos (varios escriben o borran en storage — `recreate-bucket.js` y `rename-images.js` son destructivos). En su lugar, chequear que parsean sin error de sintaxis con `node --check`.

Si `storage-audit.js` funciona por el camino B, el patron es correcto y los otros 7 comparten exactamente el mismo preambulo.
  </action>
  <verify>
    <automated>node --check check-non-webp.js &amp;&amp; node --check compress-75kb.js &amp;&amp; node --check download-all-images.js &amp;&amp; node --check optimize-images.js &amp;&amp; node --check recreate-bucket.js &amp;&amp; node --check rename-images.js &amp;&amp; node --check storage-audit.js &amp;&amp; node --check storage-size.js &amp;&amp; echo "sintaxis OK 8/8"</automated>
  </verify>
  <done>
Sin la env var: `node storage-audit.js` sale con codigo 1 e imprime las instrucciones de `.env`. Con la env var: lista los buckets correctamente. Los 8 archivos pasan `node --check`. La variable de sesion quedo limpiada.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo en disco -> historial de git | El working tree se commitea; todo secreto en texto plano queda persistido |
| repo en share SMB -> otras personas | Varias personas con acceso de lectura al share pueden leer cualquier archivo del repo |
| proceso node -> Supabase | La `service_role` key bypassea todo el RLS del proyecto (541 clientes, 539 cuentas auth) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | 8 scripts con key hardcodeada | mitigate | Este plan: mover a `process.env` via `supabase-admin-client.js`; grep del prefijo JWT debe dar 0 |
| T-01-02 | Information Disclosure | `.env` nuevo commiteado por error | mitigate | `.gitignore` ya cubre `.env` y `.env.*` (verificado, lineas 15-18); `.env.example` se versiona vacio |
| T-01-03 | Elevation of Privilege | key en historial de git (commit 3dad03e) | transfer | Fuera del alcance de este plan; se resuelve por rotacion en Plan 03 (SEC-02), no por reescritura de historial |
| T-01-04 | Information Disclosure | key vieja tecleada en terminal durante el smoke test (Task 3) | accept | Queda solo en el historial de la shell de la sesion; la key se rota en Plan 03, lo que la invalida. Task 3 exige limpiar la variable |
| T-01-05 | Denial of Service | script corriendo sin key -> error opaco, alguien la vuelve a hardcodear "para probar" | mitigate | Guard con `process.exit(1)` y mensaje que apunta explicitamente a `.env.example` |
</threat_model>

<verification>
1. `grep -rn "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9" --include=*.js . --exclude-dir=node_modules` -> 0 resultados.
2. Los 8 scripts pasan `node --check`.
3. `node storage-audit.js` sin env var -> exit 1 con mensaje de `.env`.
4. `node storage-audit.js` con env var -> lista buckets.
5. `git status` no muestra ningun `.env` como archivo nuevo sin trackear.
</verification>

<success_criteria>
- SEC-01 cumplido: cero ocurrencias de la `service_role` key en el working tree.
- Los 8 scripts siguen siendo funcionales, siempre que exista `.env`.
- Existe `.env.example` documentando la variable, sin secretos.
- El Plan 03 (rotacion) puede ejecutarse sin romper nada: solo hay que pegar la key nueva en `.env`.
</success_criteria>

<output>
Al terminar, crear `.planning/phases/01-perimetro-y-deuda-critica-de-seguridad/01-01-SUMMARY.md`
</output>
