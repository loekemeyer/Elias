---
phase: 03-limpieza-y-endurecimiento-de-base-de-datos
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - migration_tn.sql
  - migration_tn_fix1.sql
  - migration_tn_fix2.sql
  - migration_tn_fix3.sql
autonomous: true
requirements: [DATA-03]

must_haves:
  truths:
    - "No queda ningun archivo migration_tn*.sql en el working tree"
    - "El borrado quedo registrado en git y es recuperable por hash"
    - "Los dos patrones de guard que contenian quedaron citados en el SUMMARY antes de borrar"
  artifacts:
    - path: ".planning/phases/03-limpieza-y-endurecimiento-de-base-de-datos/03-02-SUMMARY.md"
      provides: "Hash de recuperacion y patrones de guard extraidos"
  key_links:
    - from: "03-02-SUMMARY.md"
      to: "03-03-PLAN.md"
      via: "los patrones de guard citados alimentan la autoria del SQL de DATA-02"
---

<objective>
Eliminar del repo los 4 `migration_tn*.sql`: son artefactos muertos. No figuran en el historial
real de migraciones de Supabase (que arranca en `20260703154352 paridad_paginalk_tablas_y_rpcs`)
y contradicen el estado actual de produccion.

Purpose: mientras existan, quien lea el repo va a creer que describen la base. No lo hacen. Peor:
`migration_tn_fix2.sql:146` define `get_all_sales_lines_admin` **con** chequeo de admin, y
produccion la tiene **sin** el chequeo — leer el archivo lleva a concluir que DATA-02 ya esta
resuelto cuando no lo esta.

Output: 4 archivos borrados, un commit, y el hash para recuperarlos.

**Ejecutor: Claude, autonomo.** No toca la base de datos. Es el unico plan de la fase que no
depende del acceso de escritura al MCP — por eso corre en paralelo con el Plan 01.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md

Los 4 archivos, en la raiz del repo:
`migration_tn.sql`, `migration_tn_fix1.sql`, `migration_tn_fix2.sql`, `migration_tn_fix3.sql`

Evidencia de que estan muertos y mutuamente contradictorios:
- `migration_tn.sql:272` define `get_my_linked_customers(p_auth_user_id uuid)` — el overload
  inseguro que el Plan 04 va a dropear.
- `migration_tn_fix3.sql:138` define `get_my_linked_customers()` — el que se conserva. Ambos
  archivos conviven describiendo estados incompatibles.
- `migration_tn.sql:366` grantea `get_products_public_sorted()` a `anon`;
  `migration_tn_fix3.sql:215` grantea `get_products_public_sorted(text)` solo a `authenticated`.
  Esa discrepancia es justamente la trampa que el Plan 01 va a verificar contra produccion.
- Ninguno aparece en el historial de migraciones de Supabase.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rescatar los patrones de guard y borrar los 4 archivos</name>
  <files>migration_tn.sql, migration_tn_fix1.sql, migration_tn_fix2.sql, migration_tn_fix3.sql</files>
  <action>
**1a. Rescatar antes de borrar.** Estos archivos no sirven como descripcion de la base, pero
contienen los dos patrones de autorizacion que el Plan 03 necesita copiar. Citarlos textualmente
en el SUMMARY:

- Patron A (admin puro), de `migration_tn_fix2.sql:146`:
  `EXISTS (SELECT 1 FROM public.admins a WHERE a.auth_user_id = auth.uid())`
- Patron B (dueño-o-admin), de `migration_tn_fix2.sql:174-182` y `migration_tn_fix3.sql:44-53`:
  el bloque que acepta `customers.auth_user_id = auth.uid()` OR `user_customer_links` OR `admins`,
  y que en fix3 termina en `RAISE EXCEPTION 'Unauthorized: customer mismatch'`.

Copiar ambos bloques completos al SUMMARY. Despues del borrado son la referencia a mano.

**1b. Registrar el punto de recuperacion.** Antes de borrar:
```
git rev-parse HEAD
```
Anotar el hash en el SUMMARY junto al comando literal de restauracion:
`git show <hash>:migration_tn.sql > migration_tn.sql`

**1c. Borrar.**
```
git rm migration_tn.sql migration_tn_fix1.sql migration_tn_fix2.sql migration_tn_fix3.sql
```
Usar `git rm`, no borrado del filesystem — deja el borrado en el indice y hace explicito el commit.

**1d. Verificar que nada los referencia.**
```
findstr /S /I /M "migration_tn" *.js *.json *.md *.html *.config
```
`.planning/PROJECT.md:44` y `.planning/REQUIREMENTS.md:54` los mencionan: correcto, se quedan (son
el requisito que pide borrarlos). Cualquier OTRA referencia, sobre todo desde codigo o desde
`package.json`, reportarla en el SUMMARY — significa que algo los consumia y el borrado tiene
consecuencias no previstas.

**1e. Grep de secretos antes de commitear.** Buscar `service_role`, `eyJ` y `password` en los 4
archivos. Si hay match, escalarlo en el SUMMARY: borrarlos del working tree NO saca el secreto del
historial de git, y sigue vigente la restriccion de la Phase 1 de no publicar el repo en ningun
remoto.
  </action>
  <verify>
    <automated>powershell -Command "$f=Get-ChildItem -Path . -Filter 'migration_tn*.sql' -File -ErrorAction SilentlyContinue; if($f){throw \"quedan $($f.Count) archivos migration_tn\"}; 'OK borrados'"</automated>
    <automated>powershell -Command "$hits=Select-String -Path *.js,*.json,*.html -Pattern 'migration_tn' -ErrorAction SilentlyContinue; if($hits){throw \"referencias colgadas en codigo: $($hits.Path -join ', ')\"}; 'OK sin referencias en codigo'"</automated>
    <automated>powershell -Command "$s=git status --porcelain; if($s -match 'migration_tn' -and $s -notmatch 'D\s+migration_tn'){throw 'el borrado no quedo staged como D'}; 'OK git'"</automated>
  </verify>
  <done>
Cero archivos `migration_tn*.sql` en el working tree, el borrado staged en git, cero referencias
desde codigo, y el SUMMARY tiene el hash de recuperacion, los dos patrones de guard citados y el
resultado del grep de secretos.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo git ↔ historial | Borrar archivos cuyo contenido podria no estar replicado en otro lado |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Denial of Service | perdida de los patrones de guard necesarios para DATA-02 | mitigate | Task 1a los cita textualmente en el SUMMARY antes del borrado; `git show <hash>:<archivo>` los recupera enteros |
| T-03-05 | Info Disclosure | secretos en los .sql que sobreviven en el historial de git | mitigate | Task 1e grepea `service_role`/`eyJ`/`password` antes del commit y escala si hay match |
</threat_model>

<verification>
1. `dir migration_tn*.sql` no devuelve nada.
2. `git status` muestra los 4 como borrados.
3. Ningun `.js`, `.json` ni `.html` menciona `migration_tn`.
4. El SUMMARY tiene el hash de recuperacion y los 2 patrones de guard.
</verification>

<success_criteria>
DATA-03 cumplido: el repo ya no contiene ningun `migration_tn*.sql`, el borrado es reversible por
hash, y el contenido util migro al SUMMARY antes de desaparecer.
</success_criteria>

<output>
Crear `.planning/phases/03-limpieza-y-endurecimiento-de-base-de-datos/03-02-SUMMARY.md`.
</output>
