# Dependencias
node_modules/

# Salidas de los scripts de mantenimiento de storage
backup-images/

# Sistema
Thumbs.db
desktop.ini
.DS_Store

# Locks de edicion concurrente (estado efimero, no versionar)
.locks/

# Nunca versionar credenciales
.env
.env.*
*.pat

# ...salvo la plantilla, que no tiene secretos y documenta las variables.
# La negacion va DESPUES del patron que la excluye, si no git la ignora igual.
!.env.example
